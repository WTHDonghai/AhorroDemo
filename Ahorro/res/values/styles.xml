<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="number_button">
        <item name="android:layout_width">0dp</item>
        <item name="android:textSize">30sp</item>
        <item name="android:textColor">#123456</item>
        <item name="android:background">@drawable/key_bord_selector</item>
        <item name="android:gravity">center</item>
        <item name="android:onClick">click</item>
    </style>

    <style name="operator_button" parent="number_button">
        <item name="android:button">@null</item>
        <item name="android:background">@drawable/key_bord_operator_selector</item>
        <item name="android:layout_weight">1</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:onClick">onChecked</item>
    </style>

</resources>